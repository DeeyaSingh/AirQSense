{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Adjust this to the main file of your Flask app
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.py"
    }
  ],
  "env": {
    "FLASK_ENV": "production"
  }
}
